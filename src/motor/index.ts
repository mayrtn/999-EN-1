/**
 * Motor_Scoring — Barrel de re-exportación (Requirement 4).
 *
 * Punto único de importación del motor determinístico que normaliza la
 * telemetría y actualiza el Perfil_Jugador. Importar desde `src/motor`
 * (p. ej. `import { MotorScoring, crearPerfilInicial } from '../motor'`).
 *
 * @module motor
 */

export { MotorScoring, crearPerfilInicial } from './motorScoring';
